import styled from "styled-components";

export const Icon = styled.span`
  position: relative;
  width: 24px;
  height: 24px;
  right: -80%;
  top: -44px;
  color: rgb(0, 0, 0);
  cursor: pointer;
`;
